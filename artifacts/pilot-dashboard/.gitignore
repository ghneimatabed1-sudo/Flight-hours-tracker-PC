release

# Playwright generated outputs (Round-4 AA4, task #281).
# Reports are uploaded as workflow artifacts in
# .github/workflows/e2e-commander-provisioning.yml — never commit them.
playwright-report
test-results
.playwright

# Matrix sweep outputs (task #361). Per-cell screenshots/console/network
# logs and the parallel SPA bundle the harness builds against. Both are
# regenerated on every run; uploaded by CI as workflow artifacts.
playwright-report-matrix
test-results-matrix
test-evidence
dist-matrix
